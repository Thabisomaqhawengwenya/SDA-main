import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.emganwinisda.app',
  appName: 'Emganwini Main SDA Church',
  webDir: 'dist'
};

export default config;
